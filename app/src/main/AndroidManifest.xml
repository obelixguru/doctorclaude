<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <!-- Lets the user whitelist the app from Doze / battery optimization so monitoring keeps polling
         + alarming with the screen off. Without it, Samsung One UI (Galaxy A32) freezes the foreground
         service in standby and glucose only refreshes on unlock. -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <!-- On-phone ringing alarm for hypo/hyper + fast drops (foreground service that keeps playing). -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />
    <!-- Continuous glucose-monitoring foreground service (so polling + alarms survive backgrounding).
         specialUse (not health/dataSync): cloud-polled CGM data, must run 24/7 with no time cap and no
         runtime health-permission grant. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <!-- Restart monitoring after a reboot. -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT" />
    <!-- In-app camera (CameraX) for the product scan. -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature android:name="android.hardware.camera.any" android:required="false" />
    <!-- Health Connect (optional): read heart rate + steps if a wearable feeds them. -->
    <uses-permission android:name="android.permission.health.READ_HEART_RATE" />
    <uses-permission android:name="android.permission.health.READ_STEPS" />

    <!-- Package visibility (Android 11+): SpeechRecognizer + the camera app for the product scan. -->
    <queries>
        <intent>
            <action android:name="android.speech.RecognitionService" />
        </intent>
        <intent>
            <action android:name="android.media.action.IMAGE_CAPTURE" />
        </intent>
        <!-- Lets getSdkStatus see the Health Connect provider on Android 13 and below. -->
        <package android:name="com.google.android.apps.healthdata" />
    </queries>

    <application
        android:name=".MechabeticsApp"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.DoctorClaude"
        android:usesCleartextTraffic="false">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.DoctorClaude"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <!-- Health Connect permission rationale. Without these, the permission REQUEST silently
                 does nothing on some devices (esp. Android 13-): Health Connect refuses to show the
                 grant dialog unless the app declares a rationale target. -->
            <intent-filter>
                <action android:name="androidx.health.connect.action.SHOW_PERMISSIONS_RATIONALE" />
            </intent-filter>
            <!-- Android 14+ variant. -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW_PERMISSION_USAGE" />
                <category android:name="android.intent.category.HEALTH_PERMISSIONS" />
            </intent-filter>
        </activity>

        <!-- Lets the camera app hand back the captured product photo (scan feature). -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <!-- Looping hypo/hyper/fast-drop alarm (rings until the user taps STOP). -->
        <service
            android:name=".AlarmService"
            android:exported="false"
            android:foregroundServiceType="mediaPlayback" />

        <!-- Always-on glucose monitoring: keeps polling + evaluating alarms when the app is
             backgrounded / the screen is off / the task is swiped away. -->
        <service
            android:name=".MonitorService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="continuous_glucose_monitoring" />
        </service>

        <!-- Meal-time reminders (08/12/16/20h); each fire reschedules the next slot. -->
        <receiver
            android:name=".data.MealReminderReceiver"
            android:exported="false" />

        <!-- Doze-proof watchdog: an allow-while-idle alarm wakes the device ~every 10 min to re-assert
             monitoring (restart it if the OS killed it) + poke an immediate refresh, even in deep sleep. -->
        <receiver
            android:name=".MonitorWatchdogReceiver"
            android:exported="false" />

        <!-- Restart continuous monitoring after a device reboot / app update. -->
        <receiver
            android:name=".BootReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
            </intent-filter>
        </receiver>
    </application>
</manifest>
